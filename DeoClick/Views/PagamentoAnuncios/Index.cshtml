@model IEnumerable<DeoClick.Models.PagamentoAnuncio>

@{
    ViewData["Title"] = "Pagamento de Anúncio";
}

<h1 style="color:crimson">Lista de Pagamento de Anúncio</h1>

<p>
    <a asp-action="Create">Criar Pagamento de Anúncio</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idFormaPagamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FormaPagamento.tipoPagamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.id">Descrição</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
